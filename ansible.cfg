[defaults]
# tests/integration/test_modules.yml refers to the role by its Galaxy name, so
# the role has to be findable as a sibling directory called
# nerdygriffin.homeassistant -- which is exactly how CI checks it out, and how
# `ansible-galaxy role install` lays it out. `..` covers both.
roles_path = ..
